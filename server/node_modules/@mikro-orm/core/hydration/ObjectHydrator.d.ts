import type { AnyEntity, EntityData, EntityMetadata } from '../typings';
import { Hydrator } from './Hydrator';
import type { EntityFactory } from '../entity/EntityFactory';
declare type EntityHydrator<T> = (entity: T, data: EntityData<T>, factory: EntityFactory, newEntity: boolean, convertCustomTypes: boolean, schema?: string) => void;
export declare class ObjectHydrator extends Hydrator {
    private readonly hydrators;
    private tmpIndex;
    /**
     * @inheritDoc
     */
    hydrate<T extends AnyEntity<T>>(entity: T, meta: EntityMetadata<T>, data: EntityData<T>, factory: EntityFactory, type: 'full' | 'returning' | 'reference', newEntity?: boolean, convertCustomTypes?: boolean, schema?: string): void;
    /**
     * @inheritDoc
     */
    hydrateReference<T extends AnyEntity<T>>(entity: T, meta: EntityMetadata<T>, data: EntityData<T>, factory: EntityFactory, convertCustomTypes?: boolean, schema?: string): void;
    /**
     * @internal Highly performance-sensitive method.
     */
    getEntityHydrator<T extends AnyEntity<T>>(meta: EntityMetadata<T>, type: 'full' | 'returning' | 'reference'): EntityHydrator<T>;
    private createCollectionItemMapper;
    private wrap;
    private safeKey;
}
export {};
